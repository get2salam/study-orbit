{
  "name": "study-orbit",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-first study planner for deep work blocks, revision cycles, and exam readiness.",
  "scripts": {
    "check": "node --check js/main.js && node --check js/scoring.js && node --check js/backup.js && node --check js/coachBrief.js && node --check js/agentEval.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
